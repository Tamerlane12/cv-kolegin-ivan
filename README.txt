CV KOLEGIN IVAN — fast responsive viewer

The CV is displayed as optimized WebP images for much faster mobile loading.
The original compressed PDF is retained for downloading.
Mobile uses a ~294 KB image; desktop uses a ~954 KB image.
Search indexing is blocked with robots.txt and noindex meta tags.
